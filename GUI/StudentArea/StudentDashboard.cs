using System;
using System.Windows.Forms;

namespace VBTracker.GUI.StudentArea
{
    public partial class StudentDashboard : Form
    {
        public StudentDashboard()
        {
            InitializeComponent();
        }

       
        private void DashboardButton_Click(object sender, EventArgs e)
        {
            // Load Dashboard content into mainPanel
        }

        private void ScheduleButton_Click(object sender, EventArgs e)
        {
            // Load Schedule content into mainPanel
        }

        private void LeaveRequestButton_Click(object sender, EventArgs e)
        {
            // Load Leave Request content into mainPanel
        }

        private void NotificationButton_Click(object sender, EventArgs e)
        {
            // Load Notification content into mainPanel
        }

        private void AttendanceButton_Click(object sender, EventArgs e)
        {
            // Load Attendance content into mainPanel
        }

        private void ReportButton_Click(object sender, EventArgs e)
        {
            // Load Report content into mainPanel
        }
    }
}
